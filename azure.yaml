# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: estimator-mcp

services:
  web:
    project: ./src/EstimatorMcp.Web
    language: dotnet
    host: containerapp
    docker:
      path: ./Dockerfile
      context: .
